/** 이분검색 
 * 정렬되어 있는 배열에서
 * 탐색 범위를 절반씩 줄여가며 찾아가는 Search 방법
*/
function solution(param){
    let answer = param
    
    if(param && param.length){
        const sortedParam = param.sort((a, b) => a - b)
    }

    return answer
}

console.log(solution(param))
